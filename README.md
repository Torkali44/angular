# Angular Labs

This repository contains multiple Angular labs. Each lab is stored in its own branch as a feature.  

## Labs / Features

- **interpolation** → Lab 1: StudentApp using interpolation
- **filterProducts** → Lab 2: Courses with filtering and Bootstrap cards

---

## Development server

To start a local development server for any lab, first checkout the corresponding branch:

```bash
git checkout <lab-branch>
